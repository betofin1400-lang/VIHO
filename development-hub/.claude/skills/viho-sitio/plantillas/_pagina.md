# {{TITULO}}

<!--
  ESQUELETO de una página narrativa del sitio.

  CÓMO SE USA: copia este archivo a `<HUB>/paginas/<id>.md` con uno de los `<id>` que
  `render.py` publica (`onboarding`, `metodologia`, `arquitectura`, `e2e`, `index`) y
  escribe. El `.html` lo genera `render.py`; NO se escribe a mano y NO se versiona a mano.

  QUÉ NO PONER AQUÍ:
  - Cifras que ya salen de una tabla generada (patrones, skills, items, porcentajes).
    Se desincronizan y contradicen a la tabla que está debajo en la misma página.
  - Enlaces que salgan del sitio (`../algo.md`): la página vive en `centro-desarrollo/`,
    así que serían 404. El render los degrada a texto plano. Cita el archivo en `código`.
-->

## Resumen

{{Una o dos frases: qué es esta sección y por qué existe.}}

## {{Primer subtema}}

{{Contenido legible: prosa, listas, tablas markdown. Sin relleno.}}

## {{Segundo subtema}}

{{...}}
